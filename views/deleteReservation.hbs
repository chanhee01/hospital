<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{main_title}}</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        button {
            background-color: #f44336;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            padding: 8px 16px;
        }
    </style>
</head>
<body>
    <h1>{{main_title}}</h1>

    <table>
        <thead>
            <tr>
                <th>예약 시간</th>
                <th>진료 부서 번호</th>
                <th>취소</th>
            </tr>
        </thead>
        <tbody>
            {{#each reservations}}
                <tr>
                    <td>{{this.Reservation_Date_Time}}</td>
                    <td>{{this.Department_ID}}</td>
                    <td>
                        <form method="post" action="/deleteReservation/delete">
                            <input type="hidden" name="Reservation_Number" value="{{this.Reservation_Number}}">
                            <button type="submit">Cancel</button>
                        </form>
                    </td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>
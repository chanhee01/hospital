<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{main_title}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .container {
            max-width: 800px;
            margin: auto;
            padding: 20px;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        form {
            display: flex;
            flex-direction: column;
            max-width: 400px;
            margin: auto;
        }

        label {
            margin-bottom: 8px;
            color: #555;
        }

        input {
            padding: 10px;
            margin-bottom: 16px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        button {
            padding: 12px;
            background-color: #4caf50;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }

        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{main_title}}</h1>

        <form method="post" action="/addInpatient">
            <label for="Room_Info">Room Info:</label>
            <input type="text" id="Room_Info" name="Room_Info" required>

            <label for="Admission_Date">Admission Date and Time:</label>
            <input type="datetime-local" id="Admission_Date" name="Admission_Date" required>

            <label for="Discharge_Date_Time">Discharge Date and Time:</label>
            <input type="datetime-local" id="Discharge_Date_Time" name="Discharge_Date_Time" required>
            
            <button type="submit">Apply for Inpatient</button>
        </form>
    </div>
</body>
</html>